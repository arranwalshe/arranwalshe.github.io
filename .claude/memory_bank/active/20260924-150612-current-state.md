# Current state — 2026-09-24 (session handover)

Previous version: `archive/20260904-195432-current-state.md`.

## What was built or changed, and where it stands

Added three public pages for the Google OAuth consent screens of Arran's `gog` tools —
`/tools/`, `/tools/privacy/`, `/tools/terms/` — plus the Search Console verification file
`static/google7b966efd2a91b4bf.html`. Committed as `6c197ea` and pushed at the user's
explicit go-ahead; deployed and live. Full detail in `projects/tools-oauth-pages.md`.

The working tree still shows ` D content_backup/_index.md`. That deletion predates this
session; the user asked for it to be left alone and it was deliberately excluded from the
commit. Its fate is the user's call.

## Verified vs unverified

- **Verified**: deploy run 35994176795 succeeded; all four live URLs return 200; the live
  verification file is byte-identical to the Desktop original; the live privacy page carries
  the final wording.
- **Unverified**: the privacy page's statements that Claude-processed content isn't used for
  AI training, and that use adheres to Google's Limited Use rules; Google's current
  requirements for moving the Cloud projects out of "Testing". See the project file.

## Blockers needing the user specifically

- Search Console: click Verify for `arranwalshe.com`.
- Each Cloud project: add `arranwalshe.com` as an authorized domain, enter the three
  `/tools/` URLs, publish.
- Confirm the two unverified privacy-page claims.
- Decide what to do with the uncommitted `content_backup/_index.md` deletion.

## Next steps (ordered)

1. User completes the Search Console and consent-screen steps above.
2. If Google rejects anything on the pages, edit `content/tools/*.md` in this repo.
3. Carry-over from 2026-09-04: re-verify the 22 Amwaj entries once amwaj.media stops
   blocking fetches (see `projects/writing-page-refresh.md`).
4. Not urgent: the deploy workflow warns that its actions target the deprecated Node 20, and
   `ubuntu-latest` moves to Ubuntu 26 from 2026-10-19.

## Projects in flight

- **tools-oauth-pages** — live on production; waiting on user-side Search Console and
  consent-screen steps, plus two privacy claims to confirm.
- **writing-page-refresh** — mostly done, live; one follow-up open (Amwaj byline/URL
  spot-check). Not touched this session.
